export declare namespace config {
    /**
     * Try to locate a file in the root path of the project.
     *
     * @param {string} file
     * @param {string} defaultPath
     * @returns {string}
     */
    function getConfigFilePath(file: string, defaultLocation: string): string;
    /**
     * @deprecated use `@speedy/json-extends` instead.
     * Retrieve a JSON file. Supports `extends` with one or many existing JSON files.
     *
     * @template T
     * @param {string} filePath
     * @returns {Promise<T>}
     */
    function readConfigFile<T>(filePath: string): Promise<T>;
}
