export declare namespace packageMeta {
    /**
     * Get the version number from a `package.json`
     *
     * @param {string} [packagePath="./package.json"]
     * @returns {string}
     */
    function getVersion(packagePath?: string): string;
}
