export declare class Logger {
    private scope;
    constructor(scope?: string | undefined);
    info(message: string): void;
    /**
     * When `process.env.DEBUG` is not true debug message won't be printed.
     *
     * @param {string} method
     * @param {string} message
     * @returns
     *
     * @memberOf Logger
     */
    debug(method: string, message: string): void;
    warn(message: string): void;
    error(message?: string, error?: Error): void;
    private formatMessage(message, method?);
    private formatErrorMessage(message?, error?);
}
