{
  "name": "examples",
  "version": "1.0.0",
  "description": "We implement `HTTP-API`, `GraphQL`, `BPEL`, and `CHIML` in order to fetch data from database.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "async": "^2.6.0",
    "bpmn": "^0.2.2",
    "bpmn-engine": "^4.2.0",
    "cache-require-paths": "^0.3.0",
    "express": "^4.16.3",
    "request": "^2.85.0",
    "sqlite3": "^4.0.0"
  }
}
